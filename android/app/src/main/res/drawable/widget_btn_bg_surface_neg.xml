<?xml version="1.0" encoding="utf-8"?>
<!--
  Empty-slot background for NEGATIVE goals (avoid-success state). Clicking
  would mark the slot as a FAILURE, so hover previews red — not accent like
  widget_btn_bg_surface (which is for positive empties where click = ✓).
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_hovered="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_red_dim" />
            <stroke android:width="1dp" android:color="@color/widget_red" />
            <corners android:radius="15dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_surface" />
            <corners android:radius="15dp" />
        </shape>
    </item>
</selector>
